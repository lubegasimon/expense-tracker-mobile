import { StyleSheet } from "react-native";

const inputStyle = StyleSheet.create({
  input: {
    height: 40,
    width: 200,
    margin: 12,
    borderColor: "gray",
    borderWidth: 1,
    borderRadius: 10,
    padding: 10,
  },
});

export default inputStyle;
